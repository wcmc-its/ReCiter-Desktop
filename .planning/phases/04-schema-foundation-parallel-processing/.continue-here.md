---
context: phase
phase: 04-schema-foundation-parallel-processing
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-04-06T16:02:27.446Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Concurrent score update race — SQLAlchemy `no_autoflush` or pessimistic locking needed when two workers update the same PMID for the same person (see error below)

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Autoflush on concurrent score writes | Two workers updating the same (person_id, pmid) in person_article_score triggers `pymysql.err.OperationalError 1020 "Record has changed since last read"` — reproducible with generic-name researchers (e.g., Benjamin Anderson) who share PMIDs across different queries | blocking | Wrap score upsert block in `with db.no_autoflush:` or use `INSERT ... ON DUPLICATE KEY UPDATE` to make the operation atomic |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding.

<current_state>
Phase 04, Plan 02 automated tasks (Tasks 1–3) are committed as `d8dc5a7`. Task 4 is the human-verify checkpoint — it was run, issues were observed (see below). The user interrupted `/gsd-new-milestone` (which was starting for the pipeline UI bugs) to pause work instead.

A 04-02-SUMMARY.md was never written. That is the first thing to do on resume.

The user ran the pipeline and observed these bugs during Task 4 verification:
1. **Page heading** — "Processing Pipeline" should be "Retrieve & Score"
2. **Est. remaining timer** — increases over time (math is wrong; it should count down)
3. **Progress animation** — going the wrong direction
4. **Font size inconsistency** — "Retrieving from PubMed" and "Taking longer than usual" are different sizes and misaligned with column headers
5. **Last column stuck** — always shows "Taking longer than usual"
6. **State lost on navigation** — clicking away and returning restarts from scratch
7. **No cancel button** — user wants ability to cancel mid-run
8. **ReCiter name lookup parity** — user asked: "are you using the standard ReCiter lookup approach for people with generic names?" (unconfirmed)
9. **Number formatting** — any number ≥1000 anywhere in the app should display with commas
10. **Concurrent update error** — SQLAlchemy race condition when two workers update the same PMID for a generic-name researcher:
    ```
    (pymysql.err.OperationalError) (1020, "Record has changed since last read in table 'person_article_score'")
    [SQL: UPDATE person_article_score SET raw_score=... WHERE person_article_score.person_id = 'fh_banderso' AND ...model_type = 'feedbackIdentity']
    ```
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Pipeline runner refactor (as_completed + adaptive workers + run_id) — Done, committed `d8dc5a7`
- Task 2: Pipeline router — PipelineRun row creation + run status lifecycle — Done, committed `d8dc5a7`
- Task 3: Frontend — remove processing event, add worker count display — Done, committed `d8dc5a7`
- Plan 04-01: Alembic setup, PipelineRun model, run_id FK columns, lifespan auto-migrate — Done, committed `90a33f8`
</completed_work>

<remaining_work>

- Task 4: Human-verify checkpoint — IN PROGRESS, issues found (listed above)
- Write 04-02-SUMMARY.md (plan was executed but summary was never written)
- Decide: fix pipeline bugs in Phase 04 (extend plan) or defer to new milestone v2.1?
  - The user started `/gsd-new-milestone` for these bugs but then paused instead
  - Recommend: write summary, transition Phase 04, start new milestone for the bug fixes
</remaining_work>

<decisions_made>

- Phase 04 Plan 01: Alembic programmatic Config() (no alembic.ini), no-op baseline migration 001, NullPool in online mode
- Phase 04 Plan 02: as_completed for completion-order events; 8 workers w/ API key, 3 without; processing event type removed
- New milestone context gathered: user wants to fix pipeline UI bugs + SQLAlchemy concurrency error as dedicated milestone
</decisions_made>

<blockers>
- SQLAlchemy concurrent update error (OperationalError 1020) — blocks reliable parallel scoring for generic-name researchers. Must be fixed before Phase 04 can be marked complete or before a new pipeline run is considered stable.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-schema-foundation-parallel-processing/04-02-PLAN.md` — Task 4 criteria (what was being verified)
2. `.planning/phases/04-schema-foundation-parallel-processing/04-01-SUMMARY.md` — what Plan 01 built
3. `.planning/PROJECT.md` — milestone context and current active requirements
4. `.planning/ROADMAP.md` — phase structure, what comes after Phase 04

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: Writing PipelineRun status updates via a new SessionLocal() inside the SSE generator — works but creates N+1 DB sessions. Already done; acceptable for now but note for Phase 6 cleanup.

## Infrastructure State

- Docker Compose: unknown (user not running dev server in this session)
- Database: MariaDB with Alembic migrations 001+002 applied; `pipeline_run` table exists with at least 7 rows (run_id=7 seen in error)
- Frontend: Next.js, last built successfully (Task 3 acceptance criteria met)
- Last pipeline run: run_id=7, Benjamin Anderson scored, hit concurrent update error

## Pre-Execution Critique Required

Not applicable — pausing between verification and next planning step.

<context>
Phase 04 is functionally complete (all automated code committed). The human-verify revealed a set of real UX bugs and one critical concurrency error. The user started `/gsd-new-milestone` to capture these as a new milestone, got to Step 1 (loading context), then switched to `/gsd-pause-work` instead.

On resume: write the 04-02-SUMMARY.md, then decide whether to:
a) Run `/gsd-transition` to close Phase 04 and start Phase 05, or
b) Run `/gsd-new-milestone` to capture the UI bugs as v2.1 (while v2.0 continues in parallel)

The user's new-milestone arguments suggest they want v2.1 to be "Retrieve & Score UI/UX + Pipeline Stability" — a focused bugfix+polish milestone separate from the parity/historical work still ahead in Phases 05–08.
</context>

<next_action>
Start with: Write `.planning/phases/04-schema-foundation-parallel-processing/04-02-SUMMARY.md` capturing what Plan 02 built and the Task 4 issues observed. Then ask user: finish Phase 04 and move to Phase 05, or start new milestone v2.1 for the pipeline bugs first?
</next_action>
