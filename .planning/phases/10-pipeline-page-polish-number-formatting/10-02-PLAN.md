---
phase: 10-pipeline-page-polish-number-formatting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/pipeline-row.tsx
autonomous: false
requirements: [PIPE-04, PIPE-05, FMT-01]

must_haves:
  truths:
    - "Status text in pipeline rows uses text-xs (12px), matching UID and article count cell sizes"
    - "'Taking longer than usual' appears only for researchers whose elapsed time exceeds 2x the average — not statically in every row"
    - "Article counts in pipeline rows display with comma separators when >= 1,000"
  artifacts:
    - path: "frontend/components/pipeline-row.tsx"
      provides: "Status column font fix, article count comma formatting"
      contains: "text-xs flex items-center"
  key_links:
    - from: "frontend/components/pipeline-row.tsx"
      to: "isBottleneck prop"
      via: "Conditional rendering of 'Taking longer than usual' badge"
      pattern: "isBottleneck &&"
---

<objective>
Fix the pipeline row status column font size from text-sm to text-xs, and apply comma formatting to the articleCount display in pipeline rows.

Purpose: The status column text is visually larger than peer cells (UID, article count), creating misalignment. Article counts over 999 display without commas, inconsistent with the rest of the app. PIPE-05 (false-positive bottleneck detection) is verified as a side-effect of the ETA fix in Plan 01 — no separate code change needed, but verification is included.

Output: Updated `pipeline-row.tsx` with corrected font size and formatted article counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pipeline-page-polish-number-formatting/10-CONTEXT.md
@.planning/phases/10-pipeline-page-polish-number-formatting/10-RESEARCH.md
@.planning/phases/10-pipeline-page-polish-number-formatting/10-UI-SPEC.md

@frontend/components/pipeline-row.tsx

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From frontend/components/pipeline-row.tsx (line 54-67):
```typescript
interface PipelineRowProps {
  personId: string;
  name: string;
  phase: Phase;
  articleCount: number | null;
  scoreMin?: number;
  scoreMax?: number;
  scoreMedian?: number;
  progress?: number;
  isBottleneck?: boolean;
  log?: LogEntry[];
  expanded?: boolean;
  onToggle?: () => void;
}
```

From frontend/components/pipeline-row.tsx (line 138 — articleCount cell to FIX):
```tsx
<span className="text-sm text-gray-500 tabular-nums">{articleCount ?? "\u2014"}</span>
```

From frontend/components/pipeline-row.tsx (line 139 — status span to FIX):
```tsx
<span className={`text-sm flex items-center gap-2 ${PHASE_COLORS[phase]}`}>
```

From frontend/app/pipeline/page.tsx (lines 257-266 — bottleneck computation, for PIPE-05 verification context):
```typescript
const bottleneckIds = new Set<string>(
  avgTimePerResearcher > 0
    ? activeResearchers
        .filter((r) => {
          const rStart = researcherStartTimes[r.personId];
          return rStart !== undefined && (now - rStart) > avgTimePerResearcher * 2;
        })
        .map((r) => r.personId)
    : []
);
```
</interfaces>
</context>

<threat_model>
**Threat surface:** Frontend component display changes only. No API calls added or modified. No user input handling changes.

**Threats identified:** None at HIGH severity.
- LOW: `articleCount.toLocaleString()` with null check — safe because `!= null` catches both null and undefined, and `(0).toLocaleString()` returns `"0"` correctly.

**Conclusion:** No blocking security threats. All changes are display-only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fix status font size and articleCount formatting in pipeline-row.tsx</name>
  <files>frontend/components/pipeline-row.tsx</files>
  <read_first>
    - frontend/components/pipeline-row.tsx (full file — status span at line 139, articleCount at line 138)
    - .planning/phases/10-pipeline-page-polish-number-formatting/10-CONTEXT.md (decisions D-07, D-10, D-11)
    - .planning/phases/10-pipeline-page-polish-number-formatting/10-UI-SPEC.md (Typography section, Interaction Contract section for PIPE-04)
  </read_first>
  <action>
Make two changes to `frontend/components/pipeline-row.tsx`:

**Change 1 — Article count formatting (per D-11):**
Line 138: Replace the nullish coalescing pattern with a null-safe `.toLocaleString()` call:

Current:
```tsx
<span className="text-sm text-gray-500 tabular-nums">{articleCount ?? "\u2014"}</span>
```

Fixed:
```tsx
<span className="text-sm text-gray-500 tabular-nums">{articleCount != null ? articleCount.toLocaleString() : "\u2014"}</span>
```

IMPORTANT: Use `!= null` (loose inequality) — NOT `!== null`. Loose inequality catches both `null` and `undefined`. The value `0` is correctly handled: `0 != null` is `true`, so `(0).toLocaleString()` returns `"0"`.

**Change 2 — Status font size (per D-07):**
Line 139: Change `text-sm` to `text-xs` on the status label span:

Current:
```tsx
<span className={`text-sm flex items-center gap-2 ${PHASE_COLORS[phase]}`}>
```

Fixed:
```tsx
<span className={`text-xs flex items-center gap-2 ${PHASE_COLORS[phase]}`}>
```

Leave the spinner size (`w-3.5 h-3.5`) and `items-center` alignment unchanged. Leave the `isBottleneck` badge rendering unchanged — it already uses `text-xs`.

Do NOT change the grid column proportions (`grid-cols-[1.5fr_1fr_0.6fr_1.2fr_1fr]`) preemptively. If alignment still looks off after this fix, it will be addressed in visual verification.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/ReCiter-Desktop && grep -n "text-xs flex items-center gap-2" frontend/components/pipeline-row.tsx && grep -n "toLocaleString" frontend/components/pipeline-row.tsx && grep -c "text-sm flex items-center gap-2" frontend/components/pipeline-row.tsx | grep -q "^0$" && echo "OLD_FONT_REMOVED" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/pipeline-row.tsx status span (line ~139) contains `text-xs flex items-center gap-2` (NOT `text-sm`)
    - frontend/components/pipeline-row.tsx does NOT contain `text-sm flex items-center gap-2` anywhere (the old pattern is gone)
    - frontend/components/pipeline-row.tsx articleCount cell contains `articleCount != null ? articleCount.toLocaleString() : "\u2014"` (null-safe comma formatting)
    - frontend/components/pipeline-row.tsx does NOT contain `{articleCount ?? "\u2014"}` (the old pattern is gone)
    - The `isBottleneck` conditional rendering (`isBottleneck && <span className="text-xs text-amber-500`) is unchanged
    - The spinner size (`w-3.5 h-3.5`) is unchanged
    - `python -m pytest tests/ -x -q` exits 0 (no backend regression)
  </acceptance_criteria>
  <done>
    Status text uses text-xs (12px) matching peer cells, and articleCount displays with comma formatting for values >= 1,000.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all pipeline page fixes</name>
  <files>frontend/app/pipeline/page.tsx, frontend/components/pipeline-row.tsx, frontend/app/globals.css</files>
  <action>
Start the dev server and verify all six Phase 10 requirements visually. This checkpoint covers both Plan 01 and Plan 02 changes. The executor should start the Next.js dev server on port 3000 and navigate to the pipeline page. Use Playwright MCP to take a snapshot of http://localhost:3000/pipeline to confirm the heading and subtitle text, then take a screenshot during an active pipeline run to confirm animation direction, font sizes, ETA behavior, bottleneck badge logic, and number formatting.
  </action>
  <what-built>
    All six Phase 10 requirements implemented across Plans 01 and 02:
    - PIPE-01: Heading "Retrieve and Score" + corrected subtitle
    - PIPE-02: ETA countdown using inter-completion deltas
    - PIPE-03: Progress bar animation moves left-to-right
    - PIPE-04: Status text font size reduced to text-xs
    - PIPE-05: "Taking longer than usual" no longer appears prematurely (side-effect of ETA fix)
    - FMT-01: Comma formatting on totalArticles, totalScored (progress bar), and articleCount (pipeline rows)
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd /Users/paulalbert/Dropbox/GitHub/ReCiter-Desktop/frontend && npm run dev` (runs on port 3000)
    2. Navigate to http://localhost:3000/pipeline
    3. **PIPE-01**: Confirm the page heading reads "Retrieve and Score" (not "Processing Pipeline") and the subtitle ends with "candidate articles."
    4. **PIPE-03**: Click "Run Pipeline" to start a run. Observe the blue progress bar — stripes should animate left-to-right (forward direction).
    5. **PIPE-04**: While the pipeline runs, check the Status column text — it should be the same size as the UID column text (both 12px / text-xs), not larger.
    6. **PIPE-02**: Watch the "Est. remaining:" timer. It should decrease with each researcher completion. It may freeze between completions but must NOT count up.
    7. **PIPE-05**: Check the last column (Progress) of active rows — "Taking longer than usual" should NOT appear on every row at the start. It should only appear on a specific row after that researcher's elapsed time exceeds 2x the rolling average.
    8. **FMT-01**: If total articles or scored counts exceed 999, confirm they display with commas (e.g., "1,143 articles"). Also check the Articles column in individual pipeline rows for comma formatting.
  </how-to-verify>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/ReCiter-Desktop && grep -n "Retrieve" frontend/app/pipeline/page.tsx | head -3 && grep -n "text-xs flex items-center" frontend/components/pipeline-row.tsx && grep -n "background-position: 0 0" frontend/app/globals.css && grep -c "toLocaleString" frontend/app/pipeline/page.tsx frontend/components/pipeline-row.tsx</automated>
  </verify>
  <done>User has visually confirmed all six requirements (PIPE-01 through PIPE-05 and FMT-01) are working correctly on a live pipeline run.</done>
  <resume-signal>Type "approved" or describe any issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n "text-xs flex items-center gap-2" frontend/components/pipeline-row.tsx` — confirms PIPE-04 font fix
2. `grep -c "text-sm flex items-center gap-2" frontend/components/pipeline-row.tsx` — confirms old font removed (expect 0)
3. `grep -n "toLocaleString" frontend/components/pipeline-row.tsx` — confirms FMT-01 in row component
4. `grep -n "toLocaleString" frontend/app/pipeline/page.tsx` — confirms FMT-01 in page (expect 4+ matches)
5. Visual verification during live pipeline run covers PIPE-02, PIPE-03, PIPE-04, PIPE-05
6. `python -m pytest tests/ -x -q` — no backend regression
</verification>

<success_criteria>
- Status text in pipeline rows uses text-xs matching UID cell size (PIPE-04)
- "Taking longer than usual" only appears for genuinely slow researchers, not all rows (PIPE-05 — verified as side-effect of PIPE-02 fix)
- articleCount in pipeline rows displays with comma formatting (FMT-01 complete)
- Human verification confirms all six requirements visually
</success_criteria>

<output>
After completion, create `.planning/phases/10-pipeline-page-polish-number-formatting/10-02-SUMMARY.md`
</output>
